# 공통 코딩 규칙 (Common Coding Rules)

> 이 문서는 모든 코딩 프로젝트에 공통으로 적용되는 규칙입니다.
> 각 프로젝트의 AGENTS.md 맨 위에 이 파일을 참조하도록 명시되어 있습니다.

---

## 🧠 사고 원칙 (Think Before Code)

**가정하지 마. 혼란을 숨기지 마. 트레이드오프를 드러내.**

구현 전에:
- 가정을 명시적으로 말할 것. 불확실하면 물어볼 것.
- 해석이 여러 가지면 모두 제시할 것. 임의로 선택하지 말 것.
- 더 간단한 방법이 있으면 그걸 추천할 것.
- 불명확하면 멈추고 무엇이 혼란스러운지 말할 것.

## 🔧 단순함 우선 (Simplicity First)

**문제를 해결하는 최소한의 코드. 추측 금지.**

- 요청 외에 추가 기능 넣지 말 것.
- 일회성 코드에 추상화 넣지 말 것.
- 요청하지 않은 "유연성"이나 "설정 가능성" 넣지 말 것.
- 불가능한 시나리오에 에러 핸들링 넣지 말 것.
- 200줄 쓸 걸 50줄로 줄일 수 있으면 다시 쓸 것.

## ✂️ 수술적 변경 (Surgical Changes)

**꼭 필요한 것만 건드려. 내가 만든 쓰레기만 치워.**

기존 코드 수정 시:
- 옆에 있는 코드나 주석, 포맷팅을 "개선"하지 말 것.
- 고장 안 난 것은 리팩토링하지 말 것.
- 기존 스타일을 따를 것 (내가 다르게 하고 싶어도).
- 내 변경으로 생성된 사용하지 않는 임포트/변수/함수만 제거할 것.
- 기존 죽은 코드는 요청 없이 삭제하지 말 것.

테스트: 변경된 모든 줄은 사용자의 요청에 직접 추적 가능해야 함.

## 🎯 목표 주도 실행 (Goal-Driven Execution)

**성공 기준을 정의하고, 검증될 때까지 반복.**

작업을 검증 가능한 목표로 변환:
- "검증 추가" → "잘못된 입력 테스트를 작성하고 통과시킴"
- "버그 수정" → "재현 테스트를 작성하고 통과시킴"
- "리팩토링" → "전후 테스트 모두 통과 확인"

다단계 작업은 계획을 짜고 각 단계마다 검증:
```
1. [단계] → 검증: [확인 방법]
2. [단계] → 검증: [확인 방법]
3. [단계] → 검증: [확인 방법]
```

강한 성공 기준은 독립적 반복을 가능하게 함. 약한 기준("되게 하면")은 지속적 확인 필요.

---

## 📝 커밋 규칙 (Commit Message Rules)

### 형식
```
<type>: <description>
```

### 타입 (영어 소문자)
| 타입 | 설명 |
|---|---|
| `feat` | 새로운 기능 추가 |
| `fix` | 버그 수정, UI 수정, 오타 수정 및 세부 조정 |
| `add` | 새로운 파일이나 리소스 추가 |
| `del` | 파일/기능 삭제 |
| `refactor` | 코드 리팩토링 |
| `chore` | 패키지 매니저 설정, 빌드 작업 등 단순 관리 작업 |
| `merge` | 브랜치 병합 |

### 설명 (한국어)
- **한국어**로 작성
- 문장 끝에 마침표(`.`) 찍지 않음
- 변경 사항을 명확하고 직관적으로 설명

### 예시
```
feat: 최신설교영상 기능 추가
fix: 동영상 카드 UI 수정
add: 유튜브 재생목록 추가(주일 오후 예배, 수요예배, 새벽예배)
fix: 푸터 더미데이터 삭제 및 정보 최신화
```

---

## 🔒 보안 규칙 (Security Rules)

- API 키, 비밀번호, 토큰 등을 코드에 하드코딩하지 말 것.
- `.env` 파일은 절대 커밋하지 않을 것 (`.gitignore`에 포함 확인).
- 사용자 입력은 항상 검증/살균 처리할 것 (XSS, SQL Injection 방지).
- 민감한 정보가 로그에 출력되지 않도록 할 것.

---

## 🧪 테스트/검증 규칙 (Testing & Verification)

- 수정 후 기존 기능이 깨지지 않았는지 확인할 것.
- **프론트엔드**: 개발서버 실행 후 주요 페이지 접근 확인, 콘솔 에러 확인.
- **백엔드**: API 엔드포인트 응답 확인 (curl 또는 브라우저).
- **모바일 뷰**: 프론트엔드 수정 시 모바일 뷰에서도 정상 표시되는지 확인할 것.
- 사용자가 모바일 크롬으로 접근하므로 반드시 모바일 확인 단계를 포함할 것.

---

## 📱 모바일/반응형 규칙 (Mobile & Responsive)

- 프론트엔드 수정 시 모바일 뷰(320px ~ 375px 너비)에서 확인할 것.
- 터치 타겟 최소 44px × 44px 준수.
- 모바일에서 넘치는 콘텐츠(가로 스크롤)가 없는지 확인할 것.
- 사용자가 외부 네트워크(모바일 데이터)에서 접근하므로 로딩 성능도 고려할 것.

---

## 🔄 개발서버 관리 규칙 (Dev Server Management)

- 개발서버 실행 전 이미 같은 포트가 사용중인지 확인할 것 (`lsof -i :5173`).
- 작업 종료 후 개발서버 프로세스를 정리할 것.
- **프론트엔드**: `npm run dev` 백그라운드 실행, 작업 완료 후 종료.
- **백엔드**: PM2 `--watch` 옵션으로 코드 수정 시 자동 반영, `pm2 logs`로 확인.
- 동시에 여러 프로젝트의 개발서버를 돌리지 않음 (포트 5173 충돌 방지).

---

## 🔁 리뷰 방식 (Review Process)

**커밋 우선 → 요약 보고 방식**

1. 작업 완료 후 커밋 & 푸시를 먼저 진행.
2. 커밋 후 작업 요약을 사용자에게 보고:
   - 어떤 파일을 변경했는지
   - 무엇을 수정/추가했는지
   - 테스트/검증 결과
3. 사용자가 마음에 들지 않으면 `git revert`로 되돌릴 수 있음.

**요약 형식:**
```
📋 작업 요약
- 변경 파일: [파일 목록]
- 내용: [수정/추가한 내용]
- 검증: [확인한 사항]
- 커밋: [커밋 해시]
```

---

## 📂 프로젝트 구조 및 환경

### 프로젝트 매핑
| 프로젝트 | 로컬 경로 | 깃허브 |
|---|---|---|
| 성림 프론트엔드 | `/var/www/html/seongrim` | `github.com:ID-MEV/seongrim` |
| 백엔드 | `/var/www/html/project/mev_home-backend` | `github.com:ID-MEV/backend` |
| 개인 메모 | `/var/www/html/project/mev_home` | `github.com:ID-MEV/mev-home` |
| 성림 WP API | `/var/www/html/seongrim-api` | WordPress (Headless) |

### 기술 스택
| 프로젝트 | 스택 | 개발 명령 | 빌드 명령 |
|---|---|---|---|
| 성림 프론트 | React 19 + Vite 7 | `npm run dev` | `npm run build` → `dist/` |
| 백엔드 | Express + MariaDB + PM2 | `pm2 start server.js --watch` | 없음 (PM2 직접 실행) |
| 개인 메모 | React 19 + Vite 6 | `npm run dev` | `npm run build` → `dist/` |

### 개발서버 운영
- 작업할 때만 `npm run dev` 실행 (포트 5173 공유)
- 작업 종료 후 개발서버 중지
- 동시에 여러 프로젝트 개발서버 돌리지 않음 (포트 충돌 방지)

### 배포 구조
- 성림 프론트: `dist/` → Apache (`seongrim.o-r.kr`)
- 개인 메모: `dist/` → Apache (`mev.o-r.kr`)
- 백엔드: PM2 (port 5101) → Apache 프록시 (`api.mev.o-r.kr`)
- WP API: Apache (`api.seongrim.o-r.kr`)

---

## 🔄 작업 플로우

1. **작업 시작**: 프로젝트 디렉토리 이동 → 가정 명시 → 불명확한 점 질문
2. **코딩**: 최소한의 코드로 요청 사항만 구현, 기존 스타일 유지
3. **검증**: 변경 사항이 요구사항 충족하는지 확인 (프론트: 개발서버, 백엔드: API 테스트)
4. **커밋**: 커밋 규칙에 따라 커밋 메시지 작성 → `git add` → `git commit` → `git push`
5. **요약 보고**: 변경 파일, 내용, 검증 결과, 커밋 해시를 사용자에게 보고
6. **개발서버**: 필요 시 백그라운드 실행, 작업 완료 후 종료

---

## 📌 성림 프론트엔드 프로젝트 특이사항

### 📝 Git 커밋 메시지 규칙 (프로젝트 개별 규칙)

커밋을 생성할 때는 기존 커밋 로그 양식을 분석한 아래 규칙을 엄격하게 준수하여 작성해야 합니다.

* **형식**: `<type>: <description>` (콜론 뒤에 공백 1칸을 둡니다.)
* **타입 (Type)**: 소문자로 작성합니다.
  * `feat`: 새로운 기능 추가
  * `fix`: 버그 수정, UI 수정, 오타 수정 및 세부 조정
  * `add`: 새로운 파일이나 리소스 추가
  * `refactor`: 코드 리팩토링
  * `chore`: 패키지 매니저 설정, 빌드 작업 등 단순 관리 작업
* **본문 (Description)**:
  * **한국어**로 작성합니다.
  * 문장의 끝에 마침표(`.`)를 찍지 않습니다.
  * 변경 사항을 명확하고 직관적으로 설명합니다.

### 💡 작성 예시
* `feat: 최신설교영상 기능 추가`
* `fix: 동영상 카드 UI 수정`
* `add: 유튜브 재생목록 추가(주일 오후 예배, 수요예배, 새벽예배)`
* `fix: 푸터 더미데이터 삭제 및 정보 최신화`

### 🛠️ 하네스 엔지니어링 (Harness Engineering) 프로세스

이 프로젝트는 완성도 높은 개발을 위해 **Planer (설계자)**, **Generator (생성자)**, **Evaluator (평가자)** 3가지 역할의 에이전트 협업 구조를 적용합니다.

* **Planer (설계자)**: 코딩 작업 없이 오직 계획 및 구조 설계에만 집중. PRD 작성 및 구조화.
* **Generator (생성자)**: 설계서(PRD)를 바탕으로 실제 고품질 코드를 작성.
* **Evaluator (평가자)**: 작성된 코드가 설계서의 요구사항을 충족하는지, 버그나 스타일 이슈가 없는지 다각도로 평가.

### ✅ Chrome 임시 프로필 실행

다음 명령으로 임시 프로필을 사용한 Chrome을 실행하고 `chrome-devtools-mcp`와 자동 연결:

```bash
killall "Google Chrome"
open -na "Google Chrome" --args \
  --remote-debugging-port=9222 \
  --user-data-dir=/tmp/chrome-remote-debug \
  --no-first-run &
npm exec chrome-devtools-mcp@latest --autoConnect
```

### 🧪 EVALUATOR 검증 기준

| 평가 ID | 대상 | 통과 기준 |
|---|---|---|
| EVAL-REQ-01 | 최신 설교 & 유튜브 연동 | 최신 영상 3개 렌더링, 탭 전환, 모달 재생, 에러 없음 |
| EVAL-REQ-02 | 교회 앨범 Grid 갤러리 | 반응형 그리드, WP 데이터 렌더링, 라이트박스 모달 |
| EVAL-REQ-03 | 자유게시판 CRUD | 리스트 조회, 실시간 작성, 소프트 삭제 |
| EVAL-REQ-04 | 사이트맵 모달 | 글래스모피즘 오버레이, 스크롤 차단, 라우터 이동 |

### 📄 API 엔드포인트

**백엔드 (api.mev.o-r.kr)**:
* `GET /api/sermons/latest` — 최신 설교
* `GET /api/youtube-videos?playlistId={id}` — 유튜브 영상 목록
* `GET /api/memo` — 자유게시판 목록
* `POST /api/memo` — 게시글 작성
* `DELETE /api/memo/{id}` — 게시글 삭제
* `GET /api/weather` — 날씨

**WordPress (api.seongrim.o-r.kr)**:
* `GET /wp-json/wp/v2/posts` — 게시글 목록
* `GET /wp-json/wp/v2/posts?categories={id}` — 카테고리별 조회
* `GET /wp-json/wp/v2/media` — 미디어
* `GET /wp-json/wp/v2/categories` — 카테고리 목록
